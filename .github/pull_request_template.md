<!--
Adding your setup? Great. Fill in the two lines below and delete the rest.
Everything is checked automatically when you open this — you don't need to run
anything locally.
-->

**Link to the original post:**

**What is it:** <!-- e.g. Framework 13 running Osaka Jade on a single ultrawide -->

---

- [ ] This is my setup, and the screenshot is mine to share
- [ ] The screenshot is WebP, under 400KB, named `<id>.webp`
- [ ] If I know the theme, it's in `related_theme_ids`

<!--
CI runs the same gate the site build runs. If it goes red, read the message —
it says exactly which field is wrong and why. Common ones:

  duplicate id                 someone already has that id, pick another
  missing from public/         the image path in the record doesn't exist
  install.command must start   commands are copied from the official CLI,
    with "omarchy"             never written from memory. Leave it null.

Not adding a setup? Say what you're changing and why. Note that src/ and bot/
changes need a reason — this repo is public so people can add their desks, not
because it's looking for feature PRs.
-->
